function App() {
  const [count, setCount] = useState(0);


  const handleClick = () => {
    count = prev ;
    setCount(prev + 1);
    setCount(prev + 1);
  };

  return (
    <button onClick={handleClick}>
      {count}
    </button>
  );
}


function AddToCart(String activity){
    const[activity, setActivity] = useState("");

     const add = () => {
        <button  onClick={add}> 
        > Add </button>
     }
}




